US 8.2 - See what SmartCart saved me (AC 8.2.1-8.2.3)
US 8.2 - 显示 SmartCart 为我省下的钱（AC 8.2.1-8.2.3）

============================================================
[2026-09-14 update / 合并 US 5.1-5.4 之后的修复]
============================================================

Context / 背景
US 5.1-5.4 (Zhihao/US5.3, Zhihao/US5.4) were merged into randy/us8.2
(commit bb4b4d4). The merge committed cleanly with no leftover conflict
markers, but it exposed three gaps where Epic 5 and Epic 8 meet. Note that
the section below this banner is now partly outdated: it says the calculation
layer does not depend on US 5.4 expense records. That was true before the
merge; it no longer is.

US 5.1-5.4（Zhihao/US5.3、Zhihao/US5.4）已合并进 randy/us8.2
（提交 bb4b4d4）。合并本身干净、无残留冲突标记，但暴露出 Epic 5 与
Epic 8 衔接处的三个缺口。注意：本横幅下方的正文已部分过时——其中
「计算层不依赖 US 5.4 的消费记录」这一说法在合并前成立，现在已不成立。

Gap A+B - the trip path lost the AC 8.2.3 caveat
缺口 A+B - 行程路径丢失了 AC 8.2.3 的警示
- Teammate added ShoppingChecklist.routeProvider with the comment that the
  provenance "has to travel with the record" for AC 8.2.3. But
  tripTravelSavingsInsight returned its own narrower type with no
  routeEstimated / routeWarning, so a recorded trip could never disclose that
  its travel figure came from the straight-line fallback.
- 队友为 AC 8.2.3 在清单上加了 routeProvider，注释说来源「必须随记录传递」。
  但 tripTravelSavingsInsight 返回的是一个更窄的自有类型，没有
  routeEstimated / routeWarning，因此已记录的行程永远无法披露其差旅数字
  来自直线距离兜底。
- Fix / 修复 (frontend/lib/savings-insights.ts): merged the two travel-insight
  types into one TravelSavingsInsight; deleted the duplicate
  TripTravelSavingsInsight interface; tripTravelSavingsInsight now takes an
  optional SavingsRouteContext and prefers record.routeProvider, falling back
  to the caller context for legacy records. Unifying the type is also what lets
  one summary component render both a live list and an old trip.
  将两个差旅洞察类型合并为一个 TravelSavingsInsight；删除重复的
  TripTravelSavingsInsight 接口；tripTravelSavingsInsight 现接受可选的
  SavingsRouteContext，优先取 record.routeProvider，旧记录则回退到调用方
  上下文。类型统一也正是让同一个摘要组件既能渲染实时列表、也能渲染旧行程的原因。
- The warning TEXT is not frozen into the record: the summary renders the
  localised straight-line note itself, so an old trip still carries its caveat.
- 警示「文案」不冻结进记录：摘要自行渲染本地化的直线距离说明，因此旧行程
  仍会带上警示。

Gap C - the provenance never reached the record
缺口 C - 来源信息从未抵达记录
- TripRecord had no routeProvider and buildTripRecord never copied it, so the
  teammate's field was dead on the trip path.
- TripRecord 没有 routeProvider，buildTripRecord 也从未复制它，因此队友那个
  字段在行程路径上是死字段。
- Fix / 修复 (frontend/lib/trip-history.ts): added optional
  TripRecord.routeProvider and copied it in buildTripRecord with the same
  conditional-spread the teammate used in createShoppingChecklist. Optional by
  design: trips frozen before this field existed carry no provenance and must
  still validate (never be discarded as corrupt, AC 5.5.4).
  新增可选的 TripRecord.routeProvider，并在 buildTripRecord 中用与队友
  createShoppingChecklist 相同的条件展开写法复制它。设计为可选：早于该字段
  冻结的行程不带来源，且必须仍能通过校验（绝不因损坏而被丢弃，AC 5.5.4）。

Gap D - the chain was still broken at its source
缺口 D - 链路在源头仍是断的
- Found by tracing the call, not by reading the types: RecommendationOverview
  line 2011 called createShoppingChecklist(store, detailRows,
  { alternativeStores }) WITHOUT routeProvider, even though the component
  already receives it as a prop. So checklist.routeProvider was always
  undefined and AC 8.2.3 could never fire in a real trip.
- 靠追调用链（而非读类型）发现：RecommendationOverview 第 2011 行调用
  createShoppingChecklist 时没传 routeProvider，尽管该组件本身已把它作为
  prop 接收。于是 checklist.routeProvider 永远是 undefined，AC 8.2.3 在真实
  行程中永远无法触发。
- Fix / 修复 (frontend/components/smartcart-app.tsx): pass the existing
  routeProvider prop through to createShoppingChecklist. One parameter, no
  behaviour change to checklist creation.
  把已有的 routeProvider prop 透传给 createShoppingChecklist。仅一个参数，
  清单创建行为无任何改变。

Tests added / 新增测试
- lib/savings-insights.test.ts: +5 (straight-line caveat from the frozen
  record; google trip is not estimated; legacy record falls back to caller
  context; shared shape so one component renders both). 26 tests total.
- lib/trip-history.test.ts: +2 (buildTripRecord carries routeProvider; omits
  it for legacy callers and the record still validates).
- 行程记录中的直线距离警示；google 行程不标记为估算；旧记录回退到调用方
  上下文；共享结构使一个组件可渲染两者；buildTripRecord 复制 routeProvider，
  旧调用方则省略且记录仍能通过校验。

Also fixed by the merge / 合并顺带修复
- The pre-existing i18n.test.ts saraCategoryCandidate failure (noted in the
  earlier version of this file) is gone: the teammate's merge corrected it.
- 本文件早先版本记录的既有 i18n.test.ts saraCategoryCandidate 失败已消失：
  队友的合并修正了它。

Verification / 验证
- tsc --noEmit: OK
- vitest: 20 files, 179 tests, all passing
- eslint (6 changed files): 0 errors, 0 warnings
- next build: compiled successfully in 11.8s
- Not yet done / 尚未完成: browser walkthrough. The summary component is still
  deliberately unwired (placement is a team decision, see below), so there is
  nothing to click yet.
  浏览器实测。摘要组件仍按约定未接线（展示位置属团队决策，见下文），因此
  目前还没有可点击的界面。

Still open for the team / 仍待团队决定
- Where SavingsInsightsSummary renders (expense history page vs planner page).
- Whether AC 8.2.2 should also be computed from a recorded trip.
  AlternativeStoreEstimate already freezes estimatedTotalCostRm, so it is
  technically feasible; deliberately NOT done here because a trip record keeps
  no per-line prices for alternative stores, so the "based on N comparable
  items" disclosure required by the chosen comparison basis could not be
  honoured. Doing it anyway would mean showing a saving whose basis cannot be
  explained.
- SavingsInsightsSummary 渲染在哪里（消费历史页 vs 清单规划页）。
- AC 8.2.2 是否也应从已记录行程计算。AlternativeStoreEstimate 已冻结
  estimatedTotalCostRm，技术上可行；此处刻意未做，因为行程记录不为替代门店
  保留逐行价格，无法满足所选比价口径要求的「基于 N 件可比商品」披露。
  照做意味着展示一个无法说明依据的省钱额。

============================================================
[2026-09-14 update / 接线 SavingsInsightsSummary 到概览页]
============================================================

What was wired / 接线内容
- frontend/components/smartcart-app.tsx: imported savingsInsights and
  SavingsInsightsSummary, and rendered the summary inside
  RecommendationOverview, between the combined-total bar and the calculation
  notes. It is fed the LIVE insights (savingsInsights over store +
  alternativeStores), so AC 8.2.1 and AC 8.2.2 are both demonstrable now.
- routeWarning is passed as null on purpose: the backend's routeWarning string
  is hard-coded English, while the summary already renders the localised
  copy.straightLineTravelNote from routeEstimated (driven by routeProvider).
  Passing the raw string through would mix languages on the Malay UI. The
  caveat still shows; it just comes from i18n, not from the API.
- 在 RecommendationOverview 的合计条与计算说明之间渲染摘要，喂入「实时洞察」
  （store + alternativeStores 的 savingsInsights），因此 AC 8.2.1 与 8.2.2
  现在都可演示。routeWarning 刻意传 null：后端该字符串是硬编码英文，而摘要已
  根据 routeEstimated（由 routeProvider 驱动）渲染本地化的
  copy.straightLineTravelNote；直接透传会在马来语界面混杂语言。警示仍会显示，
  只是来自 i18n 而非 API。

Why the overview page and not the expense-history page / 为何接在概览页而非消费历史页
- US 5.5 (trip history list UI) is not built yet: tripHistory is only read for
  alreadyRecorded, there is no history screen to render into. The overview is
  the only place that currently exists AND holds store + alternativeStores +
  routeProvider together.
- AC 8.2.1 says "Given I shopped at a store" — the recommendation overview is
  exactly the moment the shopper is choosing/confirming that store.
- US 5.5（行程历史列表界面）尚未实现：tripHistory 目前只被 alreadyRecorded
  读取，没有历史页面可渲染。概览页是当前唯一既存在、又同时持有 store +
  alternativeStores + routeProvider 的位置。AC 8.2.1 写的是「Given I shopped
  at a store」，而推荐概览页正是购物者选择/确认该门店的时刻。

Trip-record path (AC 8.2.1 only, not wired to UI yet) / 行程记录路径（仅 8.2.1，尚未接线到界面）
- tripTravelSavingsInsight(record) is implemented and tested for the frozen
  trip path, but nothing renders it yet because there is no history screen.
  When US 5.5 lands, that screen should call tripTravelSavingsInsight per
  record and can reuse SavingsInsightsSummary unchanged (the unified
  TravelSavingsInsight type is what makes this possible).
- tripTravelSavingsInsight(record) 已为冻结行程路径实现并测试，但尚无界面渲染
  它，因为还没有历史页面。待 US 5.5 落地，该页面应对每条记录调用
  tripTravelSavingsInsight，并可原样复用 SavingsInsightsSummary（统一的
  TravelSavingsInsight 类型正是这一点得以成立的原因）。

Verification / 验证
- tsc --noEmit: OK; eslint (changed files): 0/0; vitest: 20 files, 179 tests
  all passing; next build: compiled successfully in 12.4s.

============================================================

[What this change does / 本次提交内容]

Frontend - calculation layer (steps 1, 2, 3)
- New frontend/lib/savings-insights.ts, a pure module with no React and no
  API calls. It reads only the StoreRecommendation list that Epic 2 already
  returns, so it does not depend on US 5.4 expense records (not yet merged
  into this branch).
- travelSavingsInsight (AC 8.2.1): my store's estimatedRoundTripCostRm minus
  the cheapest reachable alternative's. Stores carrying exceedsLimit are
  excluded from the candidate pool, because they were returned only when
  nothing matched inside the travel limit and recommending one as a saving
  would be unusable for a household that cannot travel that far.
- potentialPriceSavingsInsight (AC 8.2.2): comparison basis is the best
  single store (one trip, achievable), not a per-line theoretical minimum
  spread across several stores. Only basket lines priced at BOTH stores are
  compared, so a line my store has no price for can never read as a saving I
  never had. The result exposes comparableLineCount / basketLineCount so the
  UI can state "based on N comparable items".
- Lines whose price came from the cached cross-store median are counted
  (medianLineCount) and disclosed, following the existing store/median split
  convention in recommendation-detail.ts.
- Both insights return a structured state instead of a bare number:
  available, savingsRm, reason, and the store names. Savings of zero or less
  are reported as unavailable rather than shown as "you saved RM0", which
  would read as a result when no cheaper option exists.
- AC 8.2.3: each insight carries kind: "estimate" | "potential". Route
  metadata (routeProvider, routeWarning) travels with the travel insight, so
  the straight-line fallback cannot be displayed without its caveat.

前端 - 计算层（步骤 1、2、3）
- 新增 frontend/lib/savings-insights.ts，纯函数模块，不含 React、不发
  任何请求。它只读取 Epic 2 已返回的 StoreRecommendation 列表，因此
  不依赖 US 5.4 的消费记录（该代码尚未合并进本分支）。
- travelSavingsInsight（AC 8.2.1）：我的门店 estimatedRoundTripCostRm
  减去最便宜的可达替代门店的同一字段。带 exceedsLimit 的门店被排除在
  候选池之外——这类门店只在出行限制内无匹配时才会返回，把它当作省钱
  推荐给走不到那么远的家庭并不成立。
- potentialPriceSavingsInsight（AC 8.2.2）：比价口径为「最优单店」
  （一次行程、可执行），而非跨多店逐行取理论最低价。只比较两家店
  「都有价格」的购物篮行，因此我的店缺价的商品永远不会被算成我省下
  的钱。结果暴露 comparableLineCount / basketLineCount，供界面说明
  「基于 N 件可比商品」。
- 价格来源为跨店缓存中位价的行会被单独计数（medianLineCount）并披露，
  沿用 recommendation-detail.ts 中既有的 store/median 双源约定。
- 两个洞察都返回结构化状态而非裸数字：available、savingsRm、reason 与
  门店名。省钱额为零或负数时报告为不可用，而不是显示「你省了 RM0」——
  那会在实际没有更便宜选项时被误读为一种结果。
- AC 8.2.3：每个洞察都带 kind："estimate" | "potential"。路线元数据
  （routeProvider、routeWarning）随差旅洞察一起返回，因此直线距离兜底
  不可能在不带警示的情况下被展示。

Frontend - copy and component (steps 5, 6)
- frontend/lib/i18n.ts: 13 new keys in BOTH the English and the Bahasa
  Melayu blocks (AppCopy is inferred from COPY.en, so a missing ms key is a
  compile error). Wording is fixed by the AC: travel savings are always
  "Estimated", price savings are always "Potential", and
  potentialNotActualNote states plainly that potential savings are not money
  already saved.
- New frontend/components/savings-insights.tsx: a stateless summary that
  renders whatever insights it is handed. The estimate/potential badge is
  driven by insight.kind, not chosen by the component, so the wording cannot
  drift from the calculation. Deliberately not wired into any page yet: its
  placement is decided when US 5.4 expense records land, and keeping it in a
  standalone file avoids an "defined but never used" lint warning.

前端 - 文案与组件（步骤 5、6）
- frontend/lib/i18n.ts：英文与马来文两个块各新增 13 个键（AppCopy 类型
  由 COPY.en 推导，缺 ms 键即编译失败）。措辞按 AC 固定：差旅省钱一律
  用 "Estimated"，比价省钱一律用 "Potential"，potentialNotActualNote
  明确写出潜在省钱不等于已经省下的钱。
- 新增 frontend/components/savings-insights.tsx：无状态摘要组件，给它
  数据就渲染。estimate/potential 徽章由 insight.kind 驱动，而非组件自行
  选择措辞，因此文案不会偏离计算结果。本次刻意不接线到任何页面：展示
  位置待 US 5.4 消费记录落地后再定；放在独立文件也避免了「已定义但未
  使用」的 lint 告警。

[Problems found and how they were resolved / 遇到的问题与处理]

1. Wrong expectation in my own test (my error, not a code defect).
   The case "picks the single best store" asserted a saving of RM11, but
   Store B totals 9 + 12 = 21 against my 30, which is RM9. The
   implementation was correct; the arithmetic in the test comment and
   assertion was wrong. Fixed the expectation to 9 and re-ran: 17/17 pass.

1. 我自己测试里的期望值算错（是我的笔误，不是代码缺陷）。
   用例「选取最优单店」断言省 RM11，但 Store B 合计 9 + 12 = 21，
   对比我的 30 应为省 RM9。实现是对的，测试注释与断言里的减法算错了。
   已将期望值改为 9 并重跑：17/17 通过。

2. Pre-existing test failure, NOT introduced by this change - left untouched.
   lib/i18n.test.ts > "keeps category candidates distinct from verified
   eligibility" fails because it expects COPY.en.saraCategoryCandidate to
   contain "verify", but the shipped value is "Potential SARA item".
   Evidence it predates this work: `git diff -- lib/i18n.ts` shows my edits
   are additions only (36 insertions, 0 deletions); the diff contains no
   occurrence of saraCategoryCandidate; and `git show HEAD:frontend/lib/i18n.ts`
   returns the identical value. The copy was changed without updating its
   test. It is a one-line decision (widen the copy or relax the assertion)
   that belongs to whoever owns that string, so it was left as-is and
   recorded here instead of being silently "fixed".

2. 改动前就已存在的测试失败，非本次引入 —— 未做改动。
   lib/i18n.test.ts 的 "keeps category candidates distinct from verified
   eligibility" 失败：断言要求 COPY.en.saraCategoryCandidate 含 "verify"，
   但实际值为 "Potential SARA item"。
   证明与本次无关：`git diff -- lib/i18n.ts` 显示我的改动纯为追加
   （36 行新增、0 行删除），diff 中不含 saraCategoryCandidate；且
   `git show HEAD:frontend/lib/i18n.ts` 返回完全相同的值。属于文案被改
   过但测试未同步。修法是一行的决定（改文案或放宽断言），应由该字符串
   的负责人决定，因此原样保留并在此记录，而不擅自「修好」。

3. Branch state differed from what was assumed.
   Epic 5 code (frontend/lib/shopping-checklist.ts, US 5.1-5.3) exists only
   on origin/Zhihao/US5.3 and is NOT on this branch; HEAD is fefeee9.
   Consequence: AC 8.2.1 says "Given I shopped at a store", which strictly
   read needs a recorded trip. The calculation layer therefore takes a
   StoreRecommendation as "my store" instead of an expense record, so it can
   be built and tested now; when US 5.4 lands, only a small adapter
   (record -> myStore) is needed and no signature has to change.
   Also noted: the team renamed US 3.4's `swap` to `replacement`
   (currentReplacementImpactRm in basket-state.ts, CompactSavingsFooter in
   smartcart-app.tsx). New code follows that naming.

3. 分支实际状态与预期不同。
   Epic 5 代码（frontend/lib/shopping-checklist.ts，US 5.1-5.3）只存在于
   origin/Zhihao/US5.3，不在本分支上；HEAD 为 fefeee9。
   影响：AC 8.2.1 写的是「Given I shopped at a store」，严格读需要一次
   已记录的行程。因此计算层以 StoreRecommendation 作为「我的门店」而非
   消费记录，使其现在即可构建与测试；待 US 5.4 落地，只需一个小的适配
   函数（record -> myStore），签名无需变更。
   另记：团队已将 US 3.4 的 `swap` 改名为 `replacement`（basket-state.ts
   的 currentReplacementImpactRm、smartcart-app.tsx 的 CompactSavingsFooter）。
   新代码沿用该命名。

[Update after merging Epic 5 (US 5.1-5.4) into this branch / 合并 Epic 5 后的更新]

Since the notes above were written, origin/Zhihao/US5.3 and
origin/Zhihao/US5.4 were merged into randy/us8.2 (merge commits 41aed3d and
bb4b4d4). The merge did NOT damage any US 8.2 code: `git diff --stat
4a1b0ca HEAD` over savings-insights.ts, savings-insights.test.ts and
components/savings-insights.tsx is empty, and all 13 i18n keys survived.

自上面的记录写下之后，origin/Zhihao/US5.3 与 origin/Zhihao/US5.4 已合并进
randy/us8.2（合并提交 41aed3d、bb4b4d4）。合并没有损坏任何 US 8.2 代码：
对 savings-insights.ts、savings-insights.test.ts 与 components/
savings-insights.tsx 执行 `git diff --stat 4a1b0ca HEAD` 输出为空，13 个
i18n 键也全部完好。

Changes made in this round (4):

本轮的改动（4 处）：

1. frontend/lib/shopping-checklist.test.ts:680 - fixed a build blocker that
   arrived WITH the merge, in a teammate's file.
   `const v1 = { ...v2, version: 1 }` spread an index-signature-only Record,
   and TypeScript drops the index signature in that position, inferring
   `{ version: number }` and making `v1.items` error with TS2339. Fixed by
   annotating `const v1: Record<string, unknown>`. No test logic or assertion
   was changed. Confirmed to originate from commit a2398b4 (the file did not
   exist at 4a1b0ca, verified with `git cat-file -e`), so it is a merge
   artefact blocking `pnpm build` on this branch, not a defect introduced by
   US 8.2.

1. frontend/lib/shopping-checklist.test.ts:680 —— 修复随合并带入、位于队友
   文件中的构建阻塞。
   `const v1 = { ...v2, version: 1 }` 展开了一个只含索引签名的 Record，而
   TypeScript 在该位置会丢弃索引签名，推断成 `{ version: number }`，导致
   `v1.items` 报 TS2339。修法是标注 `const v1: Record<string, unknown>`，
   未改动任何测试逻辑或断言。已确认来自提交 a2398b4（该文件在 4a1b0ca 时
   不存在，经 `git cat-file -e` 验证），属于合并带入、在本分支阻塞
   `pnpm build` 的问题，不是 US 8.2 引入的缺陷。

2. frontend/components/smartcart-app.tsx:2287 - over-limit stores are now
   excluded BEFORE being frozen into a trip record (AC 8.2.1 "reachable").
   The frozen AlternativeStoreEstimate carries no exceedsLimit flag, and the
   ranking returns over-limit stores only as an expanded-search fallback
   (backend recommendation.py:128-134). Without this filter an unreachable
   store could later be presented as a cheaper reachable alternative.
   Verified safe to change: `alternativeStores` is consumed only by
   createShoppingChecklist (smartcart-app.tsx:2011), the sole consumer in the
   whole project, so no listing or display is affected. All 168 pre-existing
   tests still passed after the change.

2. frontend/components/smartcart-app.tsx:2287 —— 在冻结进行程记录「之前」
   排除超出出行限制的门店（AC 8.2.1 的 "reachable"）。
   冻结后的 AlternativeStoreEstimate 不带 exceedsLimit 标记，而超限门店只
   是排名在扩大搜索兜底时返回的（后端 recommendation.py:128-134）。不加
   这道过滤，一家用户根本到不了的门店将来可能被当成「更便宜的可达替代店」
   呈现。已核实改动安全：`alternativeStores` 全项目仅被
   createShoppingChecklist（smartcart-app.tsx:2011）消费，不影响任何列表
   或显示。改动后原有 168 个测试仍全部通过。

3. frontend/lib/savings-insights.ts - added tripTravelSavingsInsight
   (AC 8.2.1 from a recorded trip) plus 5 tests.
   Teammates left an explicit hook for this feature in shopping-checklist.ts
   lines 12-17: "Epic 8 interface point (D8.0 / gap G4) ... they let later
   epics compare 'what if I had shopped elsewhere' on-device without
   re-querying recommendations." TripRecord already freezes my store's
   estimatedRoundTripCostRm plus each alternative store's cost, so the saving
   is a pure on-device calculation that satisfies AC 8.4.1 (no outgoing
   request). This matches AC 8.2.1's wording "Given I shopped at a store",
   which the earlier recommendation-list version did not.
   The pre-existing travelSavingsInsight (recommendation list) is kept
   unchanged for the pre-shopping view; the two coexist deliberately.

3. frontend/lib/savings-insights.ts —— 新增 tripTravelSavingsInsight
   （AC 8.2.1 的行程记录口径）及 5 个测试。
   队友在 shopping-checklist.ts 第 12-17 行为本功能留了明确的接口点：
   "Epic 8 interface point (D8.0 / gap G4) ... they let later epics compare
   'what if I had shopped elsewhere' on-device without re-querying
   recommendations."。TripRecord 已冻结我的门店 estimatedRoundTripCostRm
   与每家替代门店的费用，因此省钱额是纯端侧计算，满足 AC 8.4.1（无外发
   请求）。这也对上了 AC 8.2.1 的原文「Given I shopped at a store」——
   而早先的推荐列表口径并不对应该时态。
   原有的 travelSavingsInsight（推荐列表口径）保持不变，供购物前查看；
   两者刻意并存。

4. Self-inflicted breakage during editing, found and fixed before commit.
   Three consecutive Edit calls on savings-insights.ts accidentally deleted
   the `interface ComparableTotals {` declaration and pasted its first field
   into the preceding comment, leaving invalid syntax at line 110. Caught by
   reading the file back; restored with a single Edit and confirmed by
   `git diff` showing only the intended one-line TripRecord import. Recorded
   here because it was my error, not a defect in the merged code.

4. 编辑过程中我自己造成的破坏，已在提交前发现并修复。
   对 savings-insights.ts 连续三次 Edit 意外删除了
   `interface ComparableTotals {` 声明，并把它的第一个字段粘进了上方注释，
   在第 110 行留下非法语法。通过读回文件发现；用一次 Edit 还原，并以
   `git diff` 确认最终只剩预期的一行 TripRecord 导入。在此记录，因为这是
   我的操作失误，不是合并代码的缺陷。

Status of the notes above:

上文记录的现状更新：

- Problem 2 (saraCategoryCandidate test failure) is RESOLVED by the merge;
  the full suite is green and no action is needed from me.
- Problem 3 (Epic 5 not on this branch) is now OBSOLETE: Epic 5 is merged.
- 问题 2（saraCategoryCandidate 测试失败）已随合并解决，全量测试为绿，
  无需我处理。
- 问题 3（Epic 5 不在本分支）已失效：Epic 5 已合并。

[How it was verified after the merge / 合并后的验证方式]

- tsc --noEmit: OK
- ESLint (savings-insights.ts, savings-insights.test.ts,
  components/savings-insights.tsx, components/smartcart-app.tsx,
  shopping-checklist.test.ts): 0 errors, 0 warnings
- Unit tests: 20 files, 173 tests, all passing (was 168 before this round;
  +5 from tripTravelSavingsInsight)
- next build: Compiled successfully; routes /, /_not-found, /planner

- tsc --noEmit：通过
- ESLint（savings-insights.ts、savings-insights.test.ts、
  components/savings-insights.tsx、components/smartcart-app.tsx、
  shopping-checklist.test.ts）：0 错误、0 警告
- 单元测试：20 个文件、173 个测试全部通过（本轮之前为 168；新增 5 个
  来自 tripTravelSavingsInsight）
- next build：编译成功；路由 /、/_not-found、/planner

---

[Original verification, before the Epic 5 merge - kept for history /
合并 Epic 5 之前的原始验证记录 —— 保留以备追溯]

The figures below were measured on this work alone (17 cases, 117-test suite,
1 pre-existing failure). They are superseded by the section above; they are
kept only so the per-AC coverage rationale stays on record.

以下数字是仅针对本项工作时测得（17 个用例、117 项测试、1 个既有失败）。
它们已被上一节取代，保留只为让「按 AC 的覆盖理由」留有记录。

- Unit tests: frontend/lib/savings-insights.test.ts, 17 cases, all passing.
  Coverage per AC:
  AC 8.2.1 - difference against a cheaper reachable store; no invented
    saving when my store is already cheapest; equal costs report no saving
    rather than RM0; a cheaper store beyond the travel limit is excluded;
    the cheapest candidate wins, not merely a cheaper one; straight-line
    fallback sets routeEstimated and carries routeWarning; amounts round to
    whole sen.
  AC 8.2.2 - only lines priced at both stores are compared, with the count
    reported; best single store wins over a per-line mix across stores;
    disjoint pricing yields no comparable lines; my store's missing prices
    are not counted as savings; stores beyond the travel limit are excluded;
    median-sourced lines are counted; an empty basket yields nothing.
  AC 8.2.3 - travel and price insights keep distinct kinds; hasAny is false
    when neither applies; the route caveat survives on the travel insight.
- i18n tests: 5 new cases assert both locales expose every key, that travel
  wording contains "Estimated", that price wording contains "Potential" and
  "could save", that potentialNotActualNote contains "not money you have
  already saved", that the two badges differ in both languages, and that the
  comparison basis and mixed-price-source disclosures exist.
- Type check: tsc --noEmit clean.
- Lint: eslint clean on all five touched/added files (0 errors, 0 warnings).
- Build: next build succeeded (compiled in 69s, 5 static pages).
- Full suite: 117 tests, 116 passed, 1 failed - the single failure is the
  pre-existing saraCategoryCandidate case described above.
- Not yet verified: no browser run. The component is intentionally unwired,
  so there is nothing on screen to click until placement is decided with
  US 5.4.

- 单元测试：frontend/lib/savings-insights.test.ts 共 17 个用例，全部通过。
  按 AC 覆盖：
  AC 8.2.1 - 与更便宜可达门店的差额；我的店已最便宜时不虚构省钱；
    成本相等时报告无省钱而非 RM0；超出出行限制的更便宜门店被排除；
    取最便宜者而非任意更便宜者；直线兜底设置 routeEstimated 并携带
    routeWarning；金额四舍五入到分。
  AC 8.2.2 - 只比较两家店都有价的行并报告数量；最优单店胜过跨店逐行
    混合；价格无交集时无可比行；我的店缺价不计为省钱；超出出行限制的
    门店被排除；中位价来源的行被计数；空购物篮不产出结果。
  AC 8.2.3 - 差旅与比价洞察保持不同 kind；两者皆不适用时 hasAny 为假；
    路线警示保留在差旅洞察上。
- i18n 测试：新增 5 个用例，断言两种语言都暴露全部键、差旅措辞含
  "Estimated"、比价措辞含 "Potential" 与 "could save"、
  potentialNotActualNote 含 "not money you have already saved"、两种语言
  的徽章互不相同、以及比价基准与混合价格来源的披露存在。
- 类型检查：tsc --noEmit 通过。
- Lint：eslint 对全部 5 个新增/改动文件零错误零告警。
- 构建：next build 成功（69 秒编译，5 个静态页）。
- 全量测试：117 项，116 通过、1 失败 —— 唯一失败即上文所述的既有
  saraCategoryCandidate 用例。
- 尚未验证：未做浏览器实测。组件按设计暂未接线，因此在展示位置随
  US 5.4 确定之前，界面上没有可点击的内容。

[Files changed / 变更文件清单]

New files
- frontend/lib/savings-insights.ts            - AC 8.2.1/8.2.2/8.2.3 calculation layer (pure functions);
                                                now includes tripTravelSavingsInsight for the recorded-trip
                                                reading of AC 8.2.1
- frontend/lib/savings-insights.test.ts       - 22 unit tests (17 recommendation-list + 5 recorded-trip)
- frontend/components/savings-insights.tsx    - stateless summary component, intentionally unwired
- US8.2(AC 8.2.1-8.2.3)README.txt             - this change record

Modified files
- frontend/lib/i18n.ts                        - 13 new keys x 2 locales (additions only)
- frontend/lib/i18n.test.ts                   - 5 new copy-assertion cases (additions only)
- frontend/components/smartcart-app.tsx:2287  - exclude over-limit stores before freezing estimates
                                                into a trip record (1 filter condition tightened)
- frontend/lib/shopping-checklist.test.ts:680 - type annotation only, fixes a merge-introduced TS2339
                                                that blocked `next build`; no test logic changed

新增文件
- frontend/lib/savings-insights.ts            - AC 8.2.1/8.2.2/8.2.3 计算层（纯函数）；
                                                现已包含 tripTravelSavingsInsight，对应 AC 8.2.1
                                                的行程记录口径
- frontend/lib/savings-insights.test.ts       - 22 个单元测试（17 个推荐列表口径 + 5 个行程记录口径）
- frontend/components/savings-insights.tsx    - 无状态摘要组件，按设计暂未接线
- US8.2(AC 8.2.1-8.2.3)README.txt             - 本变更记录

改动文件
- frontend/lib/i18n.ts                        - 新增 13 个键 × 2 种语言（纯追加）
- frontend/lib/i18n.test.ts                   - 新增 5 个文案断言用例（纯追加）
- frontend/components/smartcart-app.tsx:2287  - 冻结估算进行程记录前排除超限门店
                                                （收紧 1 个过滤条件）
- frontend/lib/shopping-checklist.test.ts:680 - 仅类型标注，修复合并带入、阻塞
                                                `next build` 的 TS2339；未改动测试逻辑

[Decisions taken from the team discussion / 来自团队讨论的决定]

- Price comparison basis: best single store (option A), not a per-line
  theoretical minimum.
- Partial baskets: compare only lines priced at both stores, and state
  "based on N comparable items" in the UI.
- routeProvider === "straight_line": still show travel savings, but always
  with routeWarning and more conservative wording.
- Placement of the summary: deferred until US 5.4 records land; the
  component is built stateless ("give it data and it renders") meanwhile.
- Build order followed: step 0 -> 1 -> 2 -> 4 -> 3 -> 5 -> 6, with step 7
  (verification) run after each.

- 比价口径：最优单店（方案 A），而非跨店逐行理论最低价。
- 部分购物篮：只比较两家店都有价格的行，并在界面说明「基于 N 件可比商品」。
- routeProvider === "straight_line"：仍显示差旅省钱，但必须带 routeWarning
  且措辞更保守。
- 摘要展示位置：待 US 5.4 记录落地后再定；在此之前组件按无状态方式构建
  （给数据即渲染）。
- 执行顺序：步骤 0 → 1 → 2 → 4 → 3 → 5 → 6，每步之后执行步骤 7（验证）。
